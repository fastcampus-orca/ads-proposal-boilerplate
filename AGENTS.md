# 프로젝트 작업 지침

- 작업 전에 README의 목적·역할·저장 규칙과 회사 문서 3종을 읽는다. 문서별 저장 위치는 README를 따른다.
- 프로젝트 파악 요청에는 준비 상태만 답한다. 문서 작성·평가·전체 실행은 요청받은 단계만 수행한다.
- 작성자와 평가자를 다른 모델·별도 대화로 나눈다. 작성자는 브리프·회사 문서·작성 양식만 읽는다. 수정 때는 자기 직전 초안과 전달받은 반려 사유·수정 요청만 추가한다.
- 요구사항 정리·체크리스트·전체 평가 기록·평가 양식과 샘플은 평가자만 읽는다. 코디네이터는 평가자의 판정 요약과 수정 요청으로 진행을 관리한다. 이는 전달 맥락을 분리하는 작업 규칙이며 파일 접근 권한을 차단하는 보안 기능이 아니다.
- 코디네이터는 워커에게 이 지침과 역할에 맞는 자료만 전달한다. 평가자는 제안서를 직접 수정하지 않는다.
- 한국어로 쓰고 Markdown 굵게 표시를 사용하지 않는다. 회사 문서·브리프의 가상 조건을 외부 사실이나 실측 결과처럼 쓰지 않는다.
- samples는 다른 소재로 만든 형식 예시다. 실제 무브펫 결과나 근거로 복사하지 않는다.
- 검사는 고정한 8개 항목 전부를 매번 확인한다. Y 개수를 실제로 세고 N이 있으면 반려한다. 최대 3라운드까지 평가하고 미통과 시 이슈를 보고한다.
- 오류를 고의로 삽입하거나 반려 횟수·점수를 조작하지 않는다. 첫 평가에서 통과하면 그대로 종료한다.
- 사용자 승인 없이 브리프·회사 기준·체크리스트를 완화하지 않는다. 입력 충돌이나 필수 정보 누락은 원문과 영향을 제시해 질문한다.
- 원자료·양식·샘플을 결과물로 덮어쓰지 않는다. 라운드별 초안·평가를 보존하고 통과한 초안만 최종본으로 복사한다.
- 실행하지 않은 검증을 완료했다고 적지 않는다. 게시·업로드·추가 워커 실행은 해당 요청 범위에 있을 때만 한다.
